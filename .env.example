# Copy to .env and fill in values.

# ── AI Feedback (free, no credit card) ───────────────────────────────────────
# Primary: Google Gemini — get free key at https://aistudio.google.com
GEMINI_API_KEY=
# Callable model for this key. gemini-2.0-flash / -flash-lite return 429
# (limit: 0) on new free-tier keys, and gemini-2.5-flash/-flash-lite are being
# retired ("no longer available to new users"). gemini-3.5-flash works. Override if needed.
GEMINI_MODEL=gemini-3.5-flash

# Fallback: Groq — get free key at https://console.groq.com
GROQ_API_KEY=

# ── Azure AI Speech (pronunciation assessment, optional) ─────────────────────
# Get these from portal.azure.com (create a "Speech" resource). Without them,
# /api/pronunciation degrades gracefully to the existing Whisper-alignment
# heuristic (provider: "whisper-heuristic" in the response) — no crash, just
# less precise scoring.
AZURE_SPEECH_KEY=
AZURE_SPEECH_REGION=

# ── Supabase (free tier) ──────────────────────────────────────────────────────
# Get these from your Supabase project: Settings > API
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key-here
SUPABASE_JWT_SECRET=your-jwt-secret-here

# ── faster-whisper (transcription) ───────────────────────────────────────────
# OFF by default. Groq Whisper (above) is the primary transcriber; this is a
# local fallback that loads a multi-hundred-MB model into the worker process.
# On a memory-constrained host (e.g. Render's 512MB instances) that load gets
# the worker OOM-killed by the kernel — uncatchable, and it reaches the browser
# as a bare 502. Only set this to 1 where the instance has the headroom.
LOCAL_WHISPER_ENABLED=0
# Model size: tiny / base / small / medium / large-v3
# "small" is a good CPU default for French. Use "base" on weak machines.
WHISPER_MODEL=small
WHISPER_DEVICE=cpu
WHISPER_COMPUTE_TYPE=int8
AI_PROVIDER_TIMEOUT_SEC=25
AI_PROVIDER_RETRIES=2
IGCSE_DB_PATH=data/igcse_speaking.db

# ── CORS ─────────────────────────────────────────────────────────────────────
# Production: set to your frontend domains, comma-separated, with no trailing slash.
# Development: include http://localhost:5173
CORS_ORIGINS=http://localhost:5173,https://frenchcoach.vercel.app,https://french.beyondthebasics.me
